server:
  port: 8080
  shutdown: graceful

spring:
  main:
    banner-mode: off
  application:
    name: back
  h2:
    console:
      enabled=true:
  threads:
    virtual:
      enabled: true
  jackson:
    deserialization:
      accept-empty-string-as-null-object: true
